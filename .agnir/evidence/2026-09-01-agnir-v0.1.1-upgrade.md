# Agnir v0.1.1 compatible upgrade — 2026-09-01

## Target Project

- Project: `mattamior/skills-hub`
- Project identity: `urn:github:mattamior:skills-hub`
- authoritative ref: `main`
- pre-upgrade repository revision: `f94c3894ec9412724607febbc27b25408a9a90cc`

## Upgrade classification

The Project already used:

- Agnir Core `0.1`
- discovery profile `repository-filesystem/0.1`

No `extensions.agnir/operations` provenance was present before this upgrade. Under the Agnir upgrade contract, missing provenance is a valid legacy/earlier operational-package input and is not a reason to re-initialize.

Target stable release:

- Agnir repository release: `v0.1.1`
- source: `iorLab/agnir`
- immutable applied revision: `e9712357ab590e5c1e5357b3cf3219d07d789aff`
- exact-candidate conformance run: `33499092957` (`success`)

Because Core/profile remain `0.1` / `repository-filesystem/0.1`, the operation is classified as a **compatible operational upgrade**, not migration.

## Preservation check

The upgrade preserves:

- `project.identity: urn:github:mattamior:skills-hub`;
- Current State locator `.agnir/state.md`;
- Next Actions locator `.agnir/next-actions.md`;
- Decisions locator `.agnir/decisions.md`;
- Evidence locator `.agnir/evidence/`;
- existing durable continuity, except for the material state fact that the v0.1.1 operational package is now applied;
- unrelated `README.md` content;
- unrelated root `AGENTS.md` instructions;
- unrelated manifest content/extensions.

The only manifest addition is optional operational provenance under `extensions.agnir/operations`.

## Activation evaluation

Repository activation remains valid and unchanged:

`Project root -> AGENTS.md -> README.md / Agnir Project Instructions -> AGNIR.yaml -> declared durable continuity`

The existing README Agnir section already satisfies the v0.1.1 target repository activation contract, so no README rewrite is required. Root `AGENTS.md` remains locator-only for Agnir.

For the ChatGPT web `skills-hub` Project execution surface, the required locator-only Project Instructions were already configured during the v0.1.1 regression validation. A genuinely fresh conversation previously proved that configuration can locate this same canonical repository/ref and enter its Agnir route without a repeated bootstrap prompt. Because this upgrade does not change canonical repository, authoritative ref, `AGENTS.md` locator, README heading, Project identity, or memory locators, no execution-surface configuration change is required.

## Result

The upgrade is designed to publish the manifest provenance, this material Current State fact, and this Evidence object in one coherent repository revision. `Next Actions` and `Decisions` do not require modification because the upgrade creates no new project-wide outstanding work or durable design choice for `skills-hub`.
